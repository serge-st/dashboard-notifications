import './Notifications.css';
import { FC } from "react";

const Notifications: FC = () => {
    return (
        <div>
            Notifications page.
        </div>
    );
};

export default Notifications;